# =============================================================================
# StashAI Server - Environment Configuration Example
# =============================================================================

# Queue Configuration
QUEUE_ENABLED=true
DIRECT_MODE=false

# Huey SQLite Configuration
QUEUE_DB_PATH=./data/queue.db

# Application Configuration
PYTHONPATH=/app
LOG_LEVEL=info

# Development Mode (set to true to disable queue and run directly)
# QUEUE_ENABLED=false
# DIRECT_MODE=true

# -----------------------------------------------------------------------------
# Centralized Service URLs
# -----------------------------------------------------------------------------
# Backend will use these values; frontend should NOT send service URLs.

# FastAPI base URLs (used by internal callbacks from workers)
STASH_INTERNAL_BASE_URL=http://stash-ai-server:9998
STASH_LOCAL_BASE_URL=http://localhost:9998
STASH_LOOPBACK_BASE_URL=http://127.0.0.1:9998

# Visage service (Docker Compose service name recommended)
VISAGE_API_URL=http://visage:8000/api/predict_1

# Optional external services; leave unset if not used
#CONTENT_ANALYSIS_API_URL=http://content-analysis:5001/api/analyze
#SCENE_ANALYSIS_API_URL=http://scene-analysis:5002/api/analyze_scene